# Demo estática de la cara pública en GitHub Pages
# (https://cromilakis.github.io/kromi-kpc). La plataforma interna requiere
# servidor + Postgres y NO forma parte de esta demo (se despliega en Vercel
# en la fase Connect). Ver next.config.ts (GITHUB_PAGES=1).
name: pages

# DESACTIVADO en push (2026-07-23): la app ahora requiere servidor (ruta
# /self-assessment/informe genera el PDF con Chromium), incompatible con el
# export estático de GitHub Pages. El destino de producción es Vercel. Queda
# solo el disparo manual como referencia histórica.
on:
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 11

      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm

      - name: Instalar dependencias
        run: pnpm install --frozen-lockfile

      - name: Build estático
        env:
          GITHUB_PAGES: "1"
          NEXT_PUBLIC_STATIC_DEMO: "1"
        run: |
          pnpm build
          touch out/.nojekyll

      - uses: actions/upload-pages-artifact@v3
        with:
          path: out

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
